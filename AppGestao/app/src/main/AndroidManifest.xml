<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppGestao"
        tools:targetApi="31">

        <!-- TELA LOGIN -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppGestao">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- tela principal pos login -->
        <activity android:name=".MainScreenActivity" android:exported="false" />

        <!-- lista de riscos pendentes -->
        <activity android:name=".PendentesActivity" android:exported="false" />

        <!-- lista de riscos resolvidos -->
        <activity android:name=".ResolvidosActivity" android:exported="false" />

        <!-- tela de detalhes de risco -->
        <activity android:name=".DetalhesRiscoActivity" android:exported="false" />

        <activity android:name=".MainActivity" android:exported="false" />
    </application>

</manifest>
