<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.riskcontrol">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name="com.example.loginmatheus.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.loginmatheus.ui.LoginActivity" />
        <activity android:name="com.example.loginmatheus.ui.CadastroActivity" />
        <activity android:name="com.example.loginmatheus.ui.MainScreenActivity" />
        <activity android:name="com.example.loginmatheus.ui.RegisterRiskActivity" />
        <activity android:name="com.example.loginmatheus.ui.MapaActivity" />
    </application>

</manifest>
